{
  "config": {
    "sort-packages": true
  },
  "require": {
    "php": ">=7.0.0"
  },
  "autoload": {
    "psr-4": {
      "classes\\": "classes/"
    }
  },
  "require-dev": {
    "phpunit/phpunit": "^8.5"
  },
  "scripts": {
    "serve": "php -S localhost:8080",
    "test": "phpunit --colors=always"
  }
}